import { Gruppo, Fugaz_One } from 'next/font/google';

export const gruppo = Gruppo({ subsets: ['latin'], weight: '400' });
export const fugaz_One = Fugaz_One({
  subsets: ['latin'],
  weight: '400',
});
